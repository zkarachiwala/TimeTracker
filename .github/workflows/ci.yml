name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check for code changes
        id: changes
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE="origin/${{ github.base_ref }}"
          else
            BASE="HEAD~1"
          fi
          if git diff --name-only $BASE...HEAD 2>/dev/null | grep -qE '\.(cs|razor|csproj|sln|css|props|targets|json)$|^\.github/workflows/'; then
            echo "code=true" >> $GITHUB_OUTPUT
          else
            echo "code=false" >> $GITHUB_OUTPUT
            echo "No code changes — skipping build."
          fi

      - name: Setup .NET
        if: steps.changes.outputs.code == 'true'
        uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json

      - name: Restore
        if: steps.changes.outputs.code == 'true'
        run: dotnet restore TimeTracker.sln --locked-mode

      - name: Build
        if: steps.changes.outputs.code == 'true'
        run: dotnet build TimeTracker.sln --no-restore --configuration Release

      - name: Test
        if: steps.changes.outputs.code == 'true'
        run: dotnet test TimeTracker.Tests/TimeTracker.Tests.csproj --no-build --configuration Release --logger "console;verbosity=normal"

      - name: Check for vulnerable packages
        if: steps.changes.outputs.code == 'true'
        run: dotnet list package --vulnerable --include-transitive
